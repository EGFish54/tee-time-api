services:
  - type: web
    name: tee-time-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && playwright install  # rebuild
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
